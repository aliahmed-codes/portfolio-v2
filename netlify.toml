[build]
  command = "gatsby build"
  publish = "public"

[build.environment]
  NODE_VERSION = "16"
  NPM_VERSION = "8"
  YARN_VERSION = "1.22.19"

# Gatsby needs this for filesystem paths
[[plugins]]
  package = "@netlify/plugin-gatsby"
